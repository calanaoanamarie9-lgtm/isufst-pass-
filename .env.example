APP_NAME=ISUFSTPASS
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://your-production-domain.example.com

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# ---------------------------------------------------------------
# Supabase PostgreSQL (session pooler)
# Copy values from Supabase Dashboard > Connect > Session pooler
# Never commit real values. Set them in the host environment.
# ---------------------------------------------------------------
DB_CONNECTION=pgsql
DB_HOST=aws-0-ap-southeast-1.pooler.supabase.com
DB_PORT=6543
DB_DATABASE=postgres
DB_USERNAME=postgres.<your-project-ref>
DB_PASSWORD=
DB_SSLMODE=require

# Absolute production HTTPS base URL embedded inside generated
# student QR codes. MUST be the public domain (not an IP).
QR_URL=https://your-production-domain.example.com

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
# No queue worker runs on serverless (Vercel scales to zero), so jobs run
# inline with the request. Database-backed reminders are triggered via the
# Vercel Cron endpoint (/cron/reminders), not the Laravel scheduler.
QUEUE_CONNECTION=sync

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# ---------------------------------------------------------------
# Cron
# Shared secret required in the X-Cron-Secret header of /cron/reminders.
# Must match the value configured next to the Vercel Cron job.
# ---------------------------------------------------------------
CRON_SECRET=

# Vercel terminates TLS at the edge; trust proxy headers so Laravel
# generates https URLs and rate-limit keys use the real client IP.
TRUSTED_PROXIES=*

# ---------------------------------------------------------------
# Mail
# Gmail SMTP requires an App Password (2FA enabled) and MAIL_FROM_ADDRESS
# must exactly match MAIL_USERNAME.
# ---------------------------------------------------------------
MAIL_MAILER=smtp
MAIL_SCHEME=null
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM_ADDRESS=
MAIL_FROM_NAME="ISUFSTPASS"

# ---------------------------------------------------------------
# Supabase Storage (S3-compatible) - avatars
# Set AVATAR_DISK=supabase in production. Supabase dashboard:
#   Storage > Buckets (create "avatars") > S3 Access Keys (generate keys)
# ---------------------------------------------------------------
AVATAR_DISK=public
SUPABASE_PROJECT_REF=your-project-ref
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_REGION=ap-southeast-1
S3_BUCKET=avatars
S3_ENDPOINT=https://your-project-ref.supabase.co/storage/v1/s3
S3_PUBLIC_URL=https://your-project-ref.supabase.co/storage/v1/object/public/avatars
S3_USE_PATH_STYLE_ENDPOINT=true

VITE_APP_NAME="ISUFSTPASS"